"use client";

import { useEffect, useState } from "react";
import { getCurrentUser } from "../actions";
import toast from "react-hot-toast";

export default function Leaderboard() {
    return <p>hello this is the leaderboard page</p>
}